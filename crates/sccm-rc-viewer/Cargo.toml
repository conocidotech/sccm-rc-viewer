[package]
name = "sccm-rc-viewer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "SCCM Remote Control viewer UI (Tauri 2 + egui canvas — see roadmap Fase 5)"

# Empty stub. UI framework choice deferred to Fase 5.
# This will become a [bin] when we get there.

[dependencies]
sccm-rc-core.workspace = true
